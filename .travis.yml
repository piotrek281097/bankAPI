language: java
jdk: oraclejdk8
sudo: true
script:
  -gradle assemble
  -gradle build
  - mvn test

install:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V

cache:
  directories:
    - .autoconf
    - $HOME/.m2